apiVersion: efs.amazonaws.com/v1
kind: EFSAccessPoint
metadata:
  name: efs-ap
spec:
  fileSystemId: {{ .Values.spec.fileSystemId }}
  rootDirectory:
    path: /
    creationInfo:
      ownerGid: 1000
      ownerUid: 1000
      permissions:
      - "750"
      - "640"
      - "770"
