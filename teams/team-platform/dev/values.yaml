labels:
  env: dev
  team: platform
spec:
  env:
  # The ID of the EFS file system that must be created in AWS 
  # separately as the driver uses the EFS for storage, but it does not create it
  ingress:
    host:
    region:
    type: alb
