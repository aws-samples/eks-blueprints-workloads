resources:
- ../../base

patches:
 - target:
      kind: Application
      annotationSelector: appSource=upstream
   path: ./patch.yaml
 - target:
      kind: Application
      annotationSelector: appSource=aws
   path: ./patch-aws.yaml